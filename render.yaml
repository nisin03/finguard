services:
  - type: web
    name: expense-service
    runtime: docker
    dockerfilePath: ./expense-service/Dockerfile
    envVars:
      - key: DB_URL
        sync: false
      - key: DB_USERNAME
        sync: false
      - key: DB_PASSWORD
        sync: false
      - key: JWT_SECRET
        sync: false
